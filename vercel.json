{
  "version": 2,
  "buildCommand": "prisma generate && vite build && tsc -p tsconfig.server.json",
  "outputDirectory": "dist/public",
  "functions": {
    "api/[...path].ts": {
      "maxDuration": 30
    }
  },
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/[...path].ts" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
